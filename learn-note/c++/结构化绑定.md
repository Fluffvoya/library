```cpp
#include <print>
#include <string>
#include <tuple>

std::tuple<int, float, std::string> func() {
  return std::make_tuple(1, 3.14, "hello world");
}

auto main() -> int {
  auto [a, b, c] = func();

  std::println("{}\t{}\t{}", a, b, c);
  return 0;
}
```